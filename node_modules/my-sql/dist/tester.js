"use strict";

var _index = require("./index");

/**
 * Tester module for demonstrations and dev testing
 */
class Tester {

    /**
     * Start Example program
     */
    start() {
        (0, _index.initSession)({
            "connectionLimit": 50,
            "acquireTimeout": 60000,
            "host": "localhost",
            "port": "3306",
            "user": "root",
            "password": "gvt123",
            "database": "MY_SQL_NPM",
            "charset": "UTF8_GENERAL_CI",
            "debug": false,
            "waitForConnections": true,
            "queueLimit": 15000
        });

        (0, _index.executeTransaction)({
            queries: [{
                query: 'SELECT * FROM ABC'
            }, {
                query: 'SELECT ID FROM ABC'
            }]
        }).then(result => {
            console.log(JSON.stringify(result));
            // endSession();
        }).catch(err => {
            console.log(err);
        });

        (0, _index.executeQuery)('SELECT * FROM ABC where ID > ?', [13]).then(result => {
            console.log(result);
        }).catch(err => {
            console.log(err);
        });
    }
} /**
   * Created by pubudud on 8/30/17.
   */

/* eslint-disable */

const tester = new Tester();

tester.start();